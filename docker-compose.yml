version: "3.8"

services:
  discord-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ariane-bot
    env_file: .env  # Automatically load your .env file
    restart: unless-stopped
